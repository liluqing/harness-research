# 原型简报: <!-- 功能名 -->

> **验证目标：** <!-- 这个原型要验证什么假设？一句话说清 -->

---

## 页面清单

<!-- 列出原型需要覆盖的页面 -->

| 页面 | 路由 | 关键元素 |
|------|------|------|
| <!-- 列表页 --> | /xxx | 搜索栏、表格、分页、操作按钮 |
| <!-- 详情页 --> | /xxx/:id | 信息展示、关联数据、操作入口 |
| <!-- 表单弹窗 --> | /xxx/new | 表单字段、实时校验、提交/取消 |

---

## 数据流

<!-- 页面之间的数据是如何流转的 -->

```
列表页 → 点击新增 → 弹窗（空表单）
列表页 → 点击编辑 → 弹窗（预填数据）→ 提交 → 刷新列表
列表页 → 点击删除 → 确认弹窗 → 删除 → 刷新列表
```

---

## 关键状态

<!-- 每个页面需要展示的状态变体 -->

| 页面 | 状态 | 触发条件 |
|------|------|------|
| 列表页 | 空态 | 无数据 |
| 列表页 | 加载态 | 接口请求中 |
| 列表页 | 错误态 | 接口异常 |
| 表单 | 校验错误 | 必填为空/格式错误 |
| 表单 | 提交中 | 按钮 loading + 禁用 |

---

## 示例数据

<!-- Agent 生成原型时使用的真实感数据，不用「测试数据」「XXX」 -->

```json
{
  "users": [
    { "id": 1, "name": "张三", "email": "zhangsan@company.com", "dept": "技术部", "role": "管理员" },
    { "id": 2, "name": "李四", "email": "lisi@company.com", "dept": "产品部", "role": "普通用户" }
  ]
}
```

---

## 原型约束

- 纯 HTML 单文件，内嵌 CSS + JS
- 不做视觉设计（无品牌色、无 logo）
- 不做交互动效
- 只展示功能逻辑和数据流转
- 用真实感示例数据
- 需覆盖关键状态（空态/错误态/加载态）
