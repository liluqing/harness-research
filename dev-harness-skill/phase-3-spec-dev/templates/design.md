# 技术设计: <!-- 功能名 -->

> **约束：** 不写具体代码。只描述「是什么」（结构、约束、规则）和「为什么」（业务规则）。

---

## 1. API 契约

<!-- 每个端点一节 -->

### POST /api/v1/xxx — <!-- 简述 -->

**Request:**

| 参数 | 类型 | 必填 | 约束 | 说明 |
|------|------|:--:|------|------|
| name | string | ✅ | <!-- 如 1~64 字符 --> | <!-- 说明 --> |
| type | int | ✅ | <!-- 枚举值范围 --> | <!-- 说明 --> |

**Response (200):**

```json
{
  "code": 0,
  "data": {
    "id": 1,
    "name": "xxx"
  },
  "message": "success"
}
```

**Error:**

| code | message | 触发条件 |
|------|---------|---------|
| 40001 | 参数校验失败 | 必填缺失或格式错误 |
| 40401 | 资源不存在 | ID 对应记录不存在 |
| 40901 | <!-- 冲突类型 --> | <!-- 触发条件 --> |

---

### GET /api/v1/xxx/{id} — <!-- 简述 -->

<!-- 同上格式 -->

---

## 2. 领域模型

### 实体关系

```
<!-- 如：User ──many-to-many──> Dept -->
```

### 实体: <!-- Entity 名称 -->

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Long | PK, 自增 | 主键 |
| name | String | NOT NULL, <!-- 约束 --> | <!-- 说明 --> |
| createdAt | Instant | NOT NULL | 创建时间 |

### 仓储: <!-- Repository 名称 -->

- `findByName(String)` → Optional
- `findByXxx(Yyy)` → <!-- 返回类型 -->

### 数据传输对象

- **XxxCreateRequest:** <!-- 字段列表 -->
- **XxxResponse:** <!-- 字段列表 -->
- **XxxUpdateRequest:** <!-- 字段列表 -->

### 关键业务规则

<!-- 业务层面的约束和规则，不是代码实现细节 -->

- <!-- 例：删除用户时，不删除部门，仅解除关联 -->
- <!-- 例：邮箱修改后需重新验证 -->

---

## 3. 数据库

### 新增表: <!-- 表名 -->

| 字段 | 类型 | 约束 | 索引 | 说明 |
|------|------|------|:--:|------|
| id | BIGINT | PK AUTO_INC | ✅ | 主键 |
| name | VARCHAR(64) | NOT NULL | | 名称 |
| created_at | DATETIME | NOT NULL | | 创建时间 |

### 新增表: <!-- 关联表名（如有） -->

| 字段 | 类型 | 约束 | 索引 | 说明 |
|------|------|------|:--:|------|
| xxx_id | BIGINT | FK→xxx.id | ✅ | 关联 ID |
| yyy_id | BIGINT | FK→yyy.id | ✅ | 关联 ID |
