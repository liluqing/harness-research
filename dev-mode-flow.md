1|# 开发模式 v2.1：从 PRD 到可运行代码的完整流程
2|
3|> **定位：** Agent 开发模式的完整操作定义（v2.1）。
4|> **前置条件：** PRD + 用户沟通记录已完成
5|> **技术栈：** 项目时确定，本流程不绑定具体框架
6|>
7|> **v2.1 更新：** Spec 阶段引入决策树（先筛再问）→ 概要确认 → 再展开详细 Spec
8|> **v2 核心改进：** 合并重叠阶段 → 文档从 7 份砍到 3 份 · TDD 从表格升级为可执行代码 · 垂直切片替代一次性瀑布
9|
10|---
11|
12|## 一、全流程总览
13|
14|```
15|输入               流程                                  输出           人介入
16|┌──────┐   ┌─────────────────────────────┐      ┌──────────────┐   ┌────┐
17|│ PRD  │   │ ① Spec 生成                  │      │ specs/xxx      │   │ 👤 │ 概要确认
18|│  +   │   │   (决策树→概要→确认→详细Spec) │      │               │   └────┘
19|│ 沟通 │   │                              │      │               │
20|└──────┘   │ ② 技术设计 (API + DLL + DB)   │      │ docs/design/   │   ┌────┐
21|           │                              │      │               │   │ 👤 │ 扫一眼
22|           │ ③ 前端设计 (仅复杂逻辑时)      │      │ docs/ui/       │   └────┘
23|           │                              │      │               │
24|           │ ┌─ 垂直切片循环 ──────────┐  │      │               │
25|           │ │ ④ 任务拆解 (本切片)      │  │      │ 任务卡片       │
26|           │ │ ⑤ 写测试代码             │  │      │ tests/         │
27|           │ │ ⑥ 跑测试 (预期全红)      │  │      │ RED 报告       │
28|           │ │ ⑦ 编码实现               │  │      │ 源码           │
29|           │ │ ⑧ 编译 + 测试通过        │  │      │ GREEN 报告     │
30|           │ │ ⑨ (可选) 集成验证        │  │      │ 验证报告       │
31|           │ └────────────────────────┘  │      │               │
32|           │                              │      │               │
33|           └──────────────────────────────┘      │  👤 PR review  │
34|                                                  │  👤 最终验收    │
35|                                                  └──────────────┘
36|```
37|
38|**核心原则：先设计后编码，按切片渐进交付，每切片 TDD 红→绿。**
39|
40|---
41|
42|## 二、各阶段详细定义
43|
44|### 阶段 ①：Spec 生成（v2.1 决策树）
45|
46|> **v2.1 关键改进：** Spec 不再一次性写完发给用户。先走决策树快速筛一遍——该 Agent 定的 Agent 定，该问用户的带选项问——然后输出几句话的 **Spec 概要**给用户确认方向，确认后再展开详细 Spec。避免遗漏关键决策导致返工，也避免事无巨细都问。
47|
48|#### ①-A. Spec 生成决策树（Agent 内部 5 步）
49|
50|```
51|STEP 1: 读取 PRD + 项目上下文
52|  → 读 PRD、已有代码、设计文档、项目约定。提取所有待决策点。
53|
54|STEP 2: 逐项走决策树 —— 这个决策点谁来定？
55|  ├── 分支 A：项目上下文可推导？
56|  │   → Agent 直接定，一句话告知方案（不提问）
57|  │   → 例：「表名用 user，跟项目已有表单数命名一致」
58|  │
59|  └── 分支 B：影响大 / 需求模糊？
60|      → 给出 2~3 个选项 + 建议，让用户选
61|      → 例：「删除用户后历史数据——A.软删除保留 B.硬删除+归档 C.仅解关联。建议 A，数据可追溯」
62|      → 注意：每次不超过 3 个决策点
63|
64|STEP 3: 汇总输出 Spec 概要（几句自然语言）
65|  → 覆盖：做什么、核心流程、关键决策、边界说明
66|  → 含：Agent 自己定的决策（顺带告知）+ 需要用户选的决策（带选项）
67|  → 不要写详细 Spec——概要通过后再展开
68|
69|STEP 4: 用户确认概要 👤
70|  → 用户确认方向、回答关键决策
71|
72|STEP 5: 展开详细 Spec
73|  → 基于确认的概要，写完整 Spec 文档
74|```
75|
76|#### ①-B. 沟通规则
77|
78|| 规则 | 说明 |
79||------|------|
80|| **能推导的 → 直接定，顺带告知** | 「技术设计阶段表名会用 xxx，跟项目已有表命名一致。」不提问，一句话带过 |
81|| **要问的 → 给选项 + 建议** | 不说「要不要做 XX」，说「XX 有 A/B/C 三种方式，建议 A，因为...。你怎么看？」 |
82|| **问之前先过脑子** | 这个决策对最终产出的影响 > 10% 才问。判断标准：影响范围 × 不可逆程度 |
83|| **每次 ≤ 3 个决策点** | 超过 3 个说明 PRD 本身不够清晰，需要回炉补充 |
84|
85|#### ①-C. Spec 概要输出格式
86|
87|Agent 在 STEP 3 发到飞书的消息示例：
88|
89|> 【Spec 概要】用户管理功能
90|>
91|> **做什么：** 新增用户管理模块，支持创建/查询/编辑/删除用户，用户可关联多个部门。
92|>
93|> **核心流程：** 管理后台 -> 用户列表页 -> 新增/编辑弹窗 -> 提交 -> 刷新列表。删除时弹出确认。
94|>
95|> **Agent 已定：** 表名单数 user；API 路径 /api/v1/users；字段校验用 Bean Validation。
96|>
97|> **需要你定（2 个）：**
98|> 1. 删除用户后历史操作日志怎么处理？A. 软删除保留 B. 硬删除+归档到日志表 C. 仅解除关联不删日志 → 建议 A
99|> 2. 用户头像这版做不做？A. 做（含图片上传+裁剪）B. 不做，仅文字信息 → 建议 B，降低 v1 范围
100|
101|#### ①-D. Spec 文档结构
102|
103|| 维度 | 内容 |
104||------|------|
105|| **Agent 帽子** | 需求分析师 |
106|| **输入** | PRD + 用户沟通记录 + 项目上下文 |
107|| **产出** | `specs/<功能名>-spec.md` |
108|| **人介入** | **必须**：STEP 3 概要发给用户确认 → STEP 5 展开详细 Spec |
109|
110|```markdown
111|# Spec: [功能名称]
112|
113|## 1. 一句话概述
114|做什么，为什么做
115|
116|## 2. 用户故事
117|- 作为[角色]，我想要[动作]，以便[价值]
118|
119|## 3. 正常流程
120|步骤化的正常业务流程
121|
122|## 4. 异常与边界
123|| 场景 | 预期行为 |
124||------|---------|
125|| 参数缺失 | 返回 400 + 明确提示 |
126|| 资源不存在 | 返回 404 |
127|
128|## 5. 验收标准
129|- [ ] AC-1: ...
130|- [ ] AC-2: ...
131|```
132|
133|---
134|
135|### 阶段 ②：技术设计（API + 领域模型 + 数据库）
136|
137|> **v2 关键改进：** 原方案的 API 契约、领域模型、DB 设计三份文档合并为一份技术设计文档。
138|> 同一信息（如字段定义）只写一次，避免四处同步。
139|
140|| 维度 | 内容 |
141||------|------|
142|| **Agent 帽子** | 技术架构师 |
143|| **输入** | Spec（已确认） |
144|| **产出** | `docs/design/<功能名>-design.md` |
145|| **人介入** | **推荐**：生成后发给用户扫一眼（2 分钟方向检查，非逐行审批） |
146|
147|**技术设计文档结构：**
148|
149|```markdown
150|# 技术设计: [功能名]
151|
152|## 1. API 契约
153|
154|### POST /api/v1/users — 创建用户
155|
156|**Request:**
157|| 参数 | 类型 | 必填 | 约束 | 说明 |
158||------|------|:--:|------|------|
159|| name | string | ✅ | 1~64 字符 | 用户名 |
160|| email | string | ✅ | 邮箱格式 | 邮箱 |
161|
162|**Response (200):**
163|```json
164|{ "code": 0, "data": { "id": 1, "name": "张三", "email": "z@x.com" } }
165|```
166|
167|**Error:**
168|| code | message | 触发条件 |
169||------|---------|---------|
170|| 40001 | 参数校验失败 | 必填缺失或格式错误 |
171|| 40901 | 邮箱已存在 | email 重复 |
172|
173|### GET /api/v1/users/{id} — 查询用户详情
174|...（同上格式）
175|
176|---
177|
178|## 2. 领域模型
179|
180|### 实体关系
181|```
182|User ──many-to-many──> Dept
183|User ──one-to-many───> UserLog
184|```
185|
186|### 实体: User
187|| 字段 | 类型 | 约束 | 说明 |
188||------|------|------|------|
189|| id | Long | PK, 自增 | 主键 |
190|| name | String | NOT NULL, 1~64, UNIQUE | 用户名 |
191|| email | String | NOT NULL, 邮箱格式, UNIQUE | 邮箱 |
192|| depts | Set\<Dept\> | 多对多 | 所属部门 |
193|| createdAt | Instant | NOT NULL | 创建时间 |
194|
195|### 仓储: UserRepository
196|- `findByName(String)` → Optional\<User\>
197|- `findByEmail(String)` → Optional\<User\>
198|- `findByDeptId(Long)` → Page\<User\>
199|
200|### 数据传输对象
201|- **UserCreateRequest:** name, email, deptIds[]
202|- **UserResponse:** id, name, email, deptIds[], createdAt
203|- **UserUpdateRequest:** name?, email?, deptIds?
204|
205|### 关键业务规则
206|- 删除用户时：级联删除关联日志，但**不**删除部门（仅解除关联）
207|- 邮箱修改后需重新验证（如有此功能）
208|
209|---
210|
211|## 3. 数据库
212|
213|### 新增表: user
214|| 字段 | 类型 | 约束 | 索引 | 说明 |
215||------|------|------|:--:|------|
216|| id | BIGINT | PK AUTO_INC | ✅ | 主键 |
217|| name | VARCHAR(64) | NOT NULL | ✅ UNIQUE | 用户名 |
218|| email | VARCHAR(128) | NOT NULL | ✅ UNIQUE | 邮箱 |
219|| created_at | DATETIME | NOT NULL | | 创建时间 |
220|
221|### 新增表: user_dept
222|| 字段 | 类型 | 约束 | 索引 | 说明 |
223||------|------|------|:--:|------|
224|| user_id | BIGINT | FK→user.id | ✅ | 用户 ID |
225|| dept_id | BIGINT | FK→dept.id | ✅ | 部门 ID |
226|| PK | (user_id, dept_id) | — | — | 联合主键 |
227|```
228|
229|**文档约束：**
230|- 不写具体代码（Java/React 代码留到编码阶段产出）
231|- 只描述「是什么」（结构、约束、规则）和「为什么」（业务规则）
232|- 字段定义在 API 和领域模型之间不一致时，以领域模型为准（API 只是视图）
233|
234|---
235|
236|### 阶段 ③：前端设计（条件性触发）
237|
238|| 维度 | 内容 |
239||------|------|
240|| **触发条件** | 同时满足：①有前端界面 ②满足以下任意一项：多步状态流转 / ≥3 个交互分支 / 实时协作 / 复杂表单校验 |
241|| **排除** | 简单列表查询、标准 CRUD 表单、单步操作 |
242|| **产出** | `docs/ui/<功能名>-ui.md` |
243|
244|```markdown
245|# 前端设计: [功能名]
246|
247|## 页面路由
248|/web/xxx
249|
250|## 组件树
251|XxxPage
252|├── XxxSearchBar (搜索条件)
253|├── XxxTable (数据展示)
254|└── XxxModal (新增/编辑弹窗)
255|
256|## 交互流程（仅复杂流程）
257|1. 进入页面 → 加载数据
258|2. 点击新增 → 弹窗 → 填写表单 → 实时校验 → 提交 → 刷新列表
259|
260|## 错误处理
261|- 网络异常：Toast 提示 + 保留表单数据
262|- 409 冲突：高亮冲突字段
263|```
264|
265|---
266|
267|### 阶段 ④~⑧：垂直切片——逐端点 TDD 交付
268|
269|> **v2 关键改进：** 不再「全部设计→全部编码→全部测试」，改为按 API 端点切片的 TDD 循环。
270|> 每个切片独立走完 ④→⑤→⑥→⑦→⑧，一个端点绿了再做下一个。
271|
272|**切片粒度标准：** 一个 API 端点 + 其依赖的最小实体/DTO/Repository 集合。
273|
274|**切片示例（用户管理功能）：**
275|```
276|切片 A: POST /api/v1/users (创建)
277|切片 B: GET /api/v1/users/{id} (查询)
278|切片 C: PUT /api/v1/users/{id} (更新)
279|切片 D: DELETE /api/v1/users/{id} (删除)
280|切片 E: GET /api/v1/users (列表 + 分页)
281|```
282|
283|---
284|
285|#### 阶段 ④：任务拆解（本切片）
286|
287|| 维度 | 内容 |
288||------|------|
289|| **Agent 帽子** | 实施工程师 |
290|| **输入** | 技术设计文档 + 当前切片范围 |
291|| **产出** | 本切片的编码任务卡片 |
292|
293|**任务卡片格式（以切片 A 为例）：**
294|
295|```markdown
296|## 切片 A: POST /users — 创建用户
297|
298|- [ ] Migration: user 表 + user_dept 表
299|- [ ] Entity: User, UserDept
300|- [ ] DTO: UserCreateRequest, UserResponse
301|- [ ] Repository: UserRepository
302|- [ ] Service: UserService.create()
303|- [ ] Controller: POST /users
304|- [ ] 参数校验
305|```
306|
307|---
308|
309|#### 阶段 ⑤：写测试代码
310|
311|| 维度 | 内容 |
312||------|------|
313|| **Agent 帽子** | 测试工程师 |
314|| **输入** | 技术设计 + 切片任务卡片 |
315|| **产出** | **可执行的测试代码**（不是 markdown 表格） |
316|
317|**测试代码结构（以切片 A 为例）：**
318|
319|```java
320|// UserServiceTest.java
321|class UserServiceTest {
322|
323|    @Mock UserRepository repo;
324|    @InjectMocks UserService service;
325|
326|    // === 正常流程 ===
327|    @Test void shouldCreateUser_whenValidInput() {
328|        // given
329|        var req = new UserCreateRequest("张三", "z@x.com", List.of(1L));
330|        when(repo.findByEmail(any())).thenReturn(Optional.empty());
331|        when(repo.save(any())).thenReturn(expectedUser);
332|
333|        // when
334|        var result = service.create(req);
335|
336|        // then
337|        assertThat(result.getName()).isEqualTo("张三");
338|        verify(repo).save(any());
339|    }
340|
341|    // === 边界条件 ===
342|    @Test void shouldReject_whenNameExceeds64Chars() { ... }
343|    @Test void shouldReject_whenEmailInvalid() { ... }
344|
345|    // === 异常流程 ===
346|    @Test void shouldReject_whenEmailExists() {
347|        when(repo.findByEmail("z@x.com")).thenReturn(Optional.of(existing));
348|        assertThrows(DuplicateException.class, () -> service.create(req));
349|    }
350|}
351|```
352|
353|**测试覆盖要求：**
354|- 每个端点至少覆盖：正常流程 + 边界条件 + 异常流程
355|- Mock 策略写在测试代码中，不用额外文档
356|
357|---
358|
359|#### 阶段 ⑥：跑测试（预期全红 — RED）
360|
361|| 维度 | 内容 |
362||------|------|
363|| **Agent 帽子** | 测试工程师 |
364|| **操作** | 运行阶段⑤的测试代码 |
365|| **预期结果** | **全部失败**（因为还没写实现代码） |
366|| **异常处理** | 如果有测试因编译错误而无法运行，先修测试代码 |
367|
368|> **这一步是 TDD 的关键仪式**——确认测试能失败，且失败原因是我们预期的（类不存在、方法未实现），而不是测试本身写错了。
369|
370|---
371|
372|#### 阶段 ⑦：编码实现
373|
374|| 维度 | 内容 |
375||------|------|
376|| **Agent 帽子** | 开发者 |
377|| **输入** | 技术设计 + 失败测试 |
378|| **产出** | 让测试变绿的最小实现代码 |
379|
380|**编码顺序（与任务卡片一致）：**
381|1. Migration（如有）
382|2. Entity
383|3. DTO
384|4. Repository
385|5. Service
386|6. Controller + 校验
387|
388|**遇到设计冲突时：**
389|- 先看技术设计文档——它是约定
390|- 如果设计确实有问题，**更新技术设计文档再继续编码**（不要悄悄偏离设计）
391|- 如果是不影响设计的实现细节选择，Agent 自主决定
392|
393|---
394|
395|#### 阶段 ⑧：编译 + 测试通过（GREEN）
396|
397|| 维度 | 内容 |
398||------|------|
399|| **Agent 帽子** | 构建工程师 + 测试工程师 |
400|| **操作** | 编译 + 运行阶段⑤的测试 |
401|| **目标** | 编译 0 error，测试全部通过 |
402|| **不通过时** | 回到阶段⑦修复代码（不是改测试！） |
403|
404|---
405|
406|#### 阶段 ⑨（可选）：集成验证
407|
408|| 维度 | 内容 |
409||------|------|
410|| **触发条件** | 所有切片完成后（或至少关键切片完成） |
411|| **操作** | 启动应用 → 实际调用 API → 验证端到端行为 |
412|| **产出** | 集成验证报告 |
413|
414|---
415|
416|## 三、人介入节点（v2.1）
417|
418|```
419|① Spec 概要       → 👤 确认概要 + 回答关键决策（必须，≤3个决策点）
420|② 技术设计        → 👤 扫一眼（推荐，非强制）
421|③ 前端设计        → 如触发，附在②后一起给人看
422|④~⑧ 切片循环     → 🤖 Agent 自主（用户可随时介入打断）
423|全部切片完成      → 👤 PR review + 验收
424|```
425|
426|| 介入点 | 频率 | 形式 | 耗时 |
427||--------|:---:|------|:--:|
428|| Spec 概要确认 | 1 次/功能 | 发概要 + 决策选项，确认方向 + 回答决策 | ~3 分钟 |
429|| 设计扫一眼 | 1 次/功能 | 发文档，方向检查 | ~2 分钟 |
430|| 切片间通知 | 每切片结束 | 「切片 X 完成，开始切片 Y」 | 0 秒（通知类） |
431|| PR review | 1 次/功能 | 完整 review | ~10 分钟 |
432|| 复杂决策 | 按需 | Agent 遇到无法判断时提问 | 不定 |
433|
434|**用户黑洞期优化：**
435|- 原方案：Spec 确认后，②~⑦ 全部黑盒，等几小时后直接看代码
436|- v2：Spec 后很快收到设计文档（扫一眼），然后每完成一个切片自动通知进度。用户始终知道「做到哪了」
437|
438|---
439|
440|## 四、文档产出清单（v2 精简版）
441|
442|| # | 文档 | 位置 | 条件 | 变更 |
443||:-:|------|------|:--:|------|
444|| ① | Spec | `specs/<功能名>-spec.md` | 必须 | 新增用户故事字段 |
445|| ② | 技术设计 | `docs/design/<功能名>-design.md` | 必须 | **合并了原 API + DLL + DB** |
446|| ③ | 前端设计 | `docs/ui/<功能名>-ui.md` | 复杂逻辑时 | 新增触发条件标准 |
447|| — | 测试代码 | `src/test/...` | 必须 | **不再是 markdown，是可执行代码** |
448|| — | 切片任务卡片 | 会话内产出 | 必须 | 每切片一张，非持久化文档 |
449|
450|**文档减少：7 → 3 份 markdown（+ 可执行测试代码）。**
451|
452|---
453|
454|## 五、v2.1 vs v1 对比总结
455|
456|| 维度 | v1 | v2.1 |
457||------|:--:|:--:|
458|| 阶段数 | 10 | 6（合并后）+ 切片循环 |
459|| 设计文档 | 7 份 markdown | 3 份 markdown |
460|| TDD | markdown 测试表格 | 可执行测试代码，红→绿 |
461|| 测试时机 | 全部编码后跑 | 编码前写，编码前先红 |
462|| 交付节奏 | 一次性瀑布 | 逐端点垂直切片 |
463|| 用户可见进度 | 首+尾两端 | Spec 概要→设计→每切片→PR |
464|| Spec 流程 | 写完整 Spec → 发用户确认 | 决策树快筛 → 概要确认 → 展开详细 Spec |
465|| Spec 问什么 | 所有模糊点都问 | 上下文可推导的 Agent 定，影响大的带选项问 |
466|| 领域模型 | 含 Java 伪代码 | 纯结构描述 |
467|| 设计错误传播距离 | 6 个阶段 | 2 个阶段（设计→切片编码） |
468|| 上下文压力 | 整功能压在一个会话 | 按切片分批，可跨会话 |
469|
470|---
471|
472|## 六、执行决策树（Agent 自检清单 v2.1）
473|
474|```mermaid
475|graph TD
476|    A[收到 PRD] --> B{PRD 清晰?}
477|    B -->|是| C[走 Spec 决策树]
478|    B -->|否| D[向用户提问澄清]
479|    C --> C1[提取决策点]
480|    C1 --> C2{决策点分类}
481|    C2 -->|上下文可推导| C3[Agent 直接定]
482|    C2 -->|影响大/模糊| C4[给选项 建议 问用户]
483|    C3 --> E[汇总 Spec 概要]
484|    C4 --> E
485|    E --> F[发用户确认概要]
486|    F --> G{用户确认?}
487|    G -->|是| H[展开详细 Spec]
488|    G -->|否| C
489|    H --> I[出技术设计]
490|    I --> J{设计需要用户看?}
491|    J -->|推荐发| K[发用户扫一眼]
492|    J -->|用户说了不用| L[直接进入切片]
493|    K --> L
494|    L --> M{还有切片未完成?}
495|    M -->|是| N[下一个切片: 任务拆解]
496|    N --> O[写测试代码]
497|    O --> P[跑测试-RED]
498|    P --> Q[编码]
499|    Q --> R[编译+测试-GREEN]
500|    R --> S{通过?}
501|    S -->|是| T[通知用户: 切片完成]
502|    S -->|否| Q
503|    T --> M
504|    M -->|否| U{需要集成验证?}
505|    U -->|是| V[集成验证]
506|    U -->|否| W[发 PR 给用户 review]
507|    V --> W
508|```
509|
510|---
511|
512|## 七、持续优化策略（实践中迭代）
513|
514|流程本身需要版本化和自我进化。以下 8 条策略在实战中持续执行，数据驱动调整。
515|
516|### 01. 记录「偏离事件」
517|
518|每当 Agent 在编码阶段发现设计文档不准确、需要临时决策并升级给人 —— 记录一次「偏离」。
519|统计偏离的类型和频率，识别 Spec 或技术设计阶段的系统性盲区。
520|
521|**指标：** 偏离次数/功能 · 偏离类型分布
522|
523|### 02. 追踪「文档返修率」
524|
525|统计 Spec 概要确认需要几轮才能通过，技术设计文档被指出多少处修正。
526|如果某个项目 Spec 平均 3+ 轮才能确认，说明 PRD 质量或 Spec 决策树模板需要改进。
527|
528|**指标：** 确认轮数 · 单次修正点数
529|
530|### 03. 监控「切片粒度」
531|
532|切片太大 → 单个切片耗时过长，反馈周期拉长。
533|切片太小 → 任务拆解开销占比过高。
534|收集每切片的实际耗时，找到「刚好够独立验证」的粒度甜点。
535|
536|**指标：** 每切片平均耗时 · 切片数/功能
537|
538|### 04. TDD 真实性审计
539|
540|定期抽查：Agent 是否真的先跑了 RED，还是跳过了直接写实现？
541|检查测试代码的覆盖率和 Mock 合理性——有没有"为了绿而绿"的假测试？
542|每 3~5 个功能做一次抽样审计。
543|
544|**指标：** RED 跳过率 · 测试覆盖率 · Mock 合理性评分
545|
546|### 05. 建立「设计模式库」
547|
548|把反复出现的领域模型、API 模式沉淀为 Skill 或参考模板。
549|例如：「标准 CRUD 设计模板」「多对多关联处理模式」「软删除设计约定」。
550|Agent 下次遇到同类需求时直接引用，减少重新推导。
551|
552|**产出：** Skill 文件 · 设计模板库
553|
554|### 06. 人机决策边界动态调整
555|
556|初期保守——多确认。跑顺畅后逐步把低风险决策下放给 Agent。
557|例如：第 1~3 个功能严格走「技术设计必须确认」，后续如果偏离率 < 5%，降级为「仅通报不阻塞」。
558|以数据驱动，而非凭感觉调整。
559|
560|**机制：** 每 5 个功能评估一次，调整决策点策略
561|
562|### 07. 流程版本管理
563|
564|流程本身是资产，需要版本化。每次调整后在 dev-mode-flow.md 头部记录版本号和变更摘要。
565|Agent 执行时读取当前版本，确保行为与文档一致。
566|实践 → 发现问题 → 更新流程 → 版本号 +1。
567|
568|**格式：** `v2.1 → v2.2 (2026-06-15) 调整切片粒度标准：单切片 > 2h 自动拆分`
569|
570|### 08. 事后回顾（Retro）
571|
572|每个功能交付后做 5 分钟轻量回顾：
573|- 流程哪个阶段最耗时？
574|- 哪个阶段的产出返工最多？
575|- Agent 有没有问了不该问的问题（该自主的没自主）？
576|- Agent 有没有没问就该问的问题（该升级的没升级）？
577|
578|**触发：** 每次 PR review 完成后自动生成回顾摘要
579|
580|---
581|
582|### 执行规划：谁、什么时候、怎么干
583|
584|| # | 策略 | 触发时机 | 执行者 | 执行方式 |
585||:-:|------|----------|:------:|------|
586|| 01 | 偏离事件 | **实时**——每次 Agent 升级决策时 | 🤖 Agent | 在升级消息里自动附加 `[偏离]` 标签 + 类型。会话结束后 Agent 汇总到 `~/.hermes/metrics/deviations/` 按功能存一条 json |
587|| 02 | 返修率 | **事后**——每功能交付时 | 🤖 Agent | Spec 确认和设计确认的轮数在会话里自然可数。PR review 后 Agent 自动统计本轮数 + 修正点，追加到同一个 json |
588|| 03 | 切片粒度 | **实时**——每切片完成时 | 🤖 Agent | 切片开始/结束时 Agent 自动打时间戳。每功能交付后汇总：总切片数、每切片耗时、中位数 |
589|| 04 | TDD 审计 | **定期**——每 3~5 个功能 | 👤 人 | Agent 不能审自己。人随机抽一个功能的 git history，看测试代码和实现代码的时间戳先后。Agent 可以辅助：自动提取该功能的 commit 时间线 |
590|| 05 | 设计模式库 | **按需**——Agent 发现重复 ≥3 次 | 🤖→👤 | Agent 发现同一模式出现 3 次以上时，提案创建一个 Skill。人审核后，Agent 生成 Skill 文件写入 `~/.hermes/skills/` |
591|| 06 | 决策边界调整 | **定期**——每 5 个功能 | 👤→🤖 | Agent 自动汇总前 5 个功能的指标（偏离率、返修率、切片耗时）。人看数据决定是否调整。人决定后 Agent 更新流程文档 |
592|| 07 | 流程版本管理 | **按需**——每次流程调整时 | 🤖 | 任何人在任何环节发现流程问题 → Agent 记录 → 讨论后更新 md → 版本号 +1。Agent 每次执行前读当前版本确认 |
593|| 08 | 事后回顾 | **事后**——每功能 PR review 完成后 | 🤖→👤 | Agent 自动生成回顾摘要（5 分钟可读完），发给人。人标注认同/异议。Agent 把结论归档到 `~/.hermes/metrics/retros/` |
594|
595|---
596|
597|### 关键执行细节
598|
599|**关于 01 偏离事件的自动记录：**
600|
601|Agent 在升级决策时，消息格式固定加上元数据：
602|
603|```
604|[偏离 · 设计冲突] 编码中发现 User 删除策略在技术设计文档里未定义。
605|当前代码假设了级联删除，但实际上可能应该是软删除。请确认。
606|```
607|
608|Agent 不需要用户手动记录——每次发 `[偏离]` 标签的消息后，会话结束时 Agent 自动提取归档。
609|
610|**关于 04 TDD 审计具体怎么做：**
611|
612|```
613|人操作（或人指挥 Agent）：
614|1. git log -- src/test/.../UserServiceTest.java  → 拿测试文件首次提交时间 T1
615|2. git log -- src/main/.../UserService.java       → 拿实现文件首次提交时间 T2
616|3. 如果 T2 < T1：跳过 RED，直接写了实现 → RED 跳过
617|4. 抽查测试代码：有没有 assert? Mock 是否合理? 有没有纯调通不做断言的测试?
618|```
619|
620|Agent 可以帮人执行步骤 1~3，但步骤 4 的判断必须人来做。
621|
622|**关于 06 决策边界调整的判断标准：**
623|
624|```
625|Agent 自动出报表：
626|  功能 1~5 概览：
627|  - 总偏离次数：3
628|  - 偏离率：0.6/功能
629|  - Spec 平均确认轮数：1.2
630|  - 设计平均修正点数：0.8
631|
632|判断标准：
633|  - 偏离率 < 0.5/功能 且 设计修正点 < 1 → 可以下放：技术设计从「必须确认」降级为「仅通报」
634|  - 偏离率 > 1.5/功能 或 Spec 平均 > 2 轮 → 需要收紧：当前决策点不够，加一个中间确认
635|```
636|
637|**关于指标存储：**
638|
639|所有指标统一存在 `~/.hermes/metrics/` 下，按项目/功能组织：
640|所有指标统一存在 `~/.hermes/metrics/` 下，按项目/功能组织：

```
~/.hermes/metrics/
├── deviations/
│   └── <项目名>/
│       └── <功能名>.json       # 偏离事件列表
├── retros/
│   └── <项目名>/
│       └── <功能名>.md          # 回顾摘要
└── summary.json                 # 汇总指标（Agent 自动更新）
```

---

### Hermes Cron 集成方案

8 条策略中，有些是**事件驱动**（Agent 编码时实时触发），有些是**定期巡检/提醒**（天然适合 cron）。
分工如下：

| 类型 | 策略 | 执行方式 |
|------|:----:|------|
| 🔴 实时 | 01 偏离事件 | Agent 编码时打标签 → 会话结束归档 json |
| 🔴 实时 | 02 返修率 | Agent 每功能交付后自动统计 → 写入 summary.json |
| 🔴 实时 | 03 切片粒度 | Agent 每切片打时间戳 → 交付后汇总 |
| 🟡 cron 提醒 | 04 TDD 审计 | cron 监控已完成功能数，到阈值提醒人 |
| 🔴 按需 | 05 设计模式库 | Agent 发现 ≥3 次重复 → 提案给人 |
| 🟡 cron 报表 | 06 决策边界调整 | cron 每 5 功能自动出指标报表，提醒人评估 |
| 🔴 按需 | 07 流程版本管理 | 任何人发现问题 → Agent 记录 → 更新文档 |
| 🟡 cron 检查 | 08 事后回顾 | cron 每天检查遗漏的 retro，提醒 Agent 生成 |

#### 计划创建的 Cron 任务

**Job 1：每周指标汇总**

```
类型: cronjob (LLM-driven)
调度: 0 9 * * 1（每周一早 9 点）
功能: 读取 ~/.hermes/metrics/summary.json，生成上周指标周报
输出: 飞书消息 → 偏离趋势 / 返修率 / 切片耗时分布 / 功能交付数
```

**Job 2：TDD 审计 + 决策边界提醒**

```
类型: script（no_agent=True，纯脚本检测） + cronjob（到阈值后 LLM 生成报表）
实现: 两步走
  Step 1 — 检测脚本（每功能交付后跑一次）:
    统计 summary.json 中已完成功能数，≥ 5 时写入触发标记文件
  Step 2 — 提醒 Agent（检测到标记后触发）:
    读取指标生成决策边界评估报表 + TDD 审计提醒，发给用户
```

**Job 3：Retro 遗漏检查**

```
类型: script（no_agent=True，纯脚本）
调度: 0 10 * * *（每天上午 10 点）
功能: 对比 summary.json 中「已交付功能列表」和 retros/ 中「已有回顾的功能列表」
      发现遗漏 → 飞书通知用户：「以下功能已交付但未做回顾：xxx, yyy」
      用户可在会话中让 Agent 补做
```

#### 数据流总览

```
Agent 编码过程                     Cron 定时任务
─────────────────                ────────────────
切片完成 → 打时间戳
偏离发生 → 写 deviations/xxx.json
功能交付 → 统计返修率 → summary.json  ──→ Job 1: 每周一早 9 点出周报
                                    ──→ Job 2: 每 5 功能出决策报表
PR review → 生成 retro/xxx.md       ──→ Job 3: 每天查遗漏 retro
```

**关键设计原则：** Agent 负责「产生数据」，Cron 负责「到点聚合和提醒」。人不被琐事打扰，但在关键节点（N 个功能后、每周一、遗漏时）自动收到提醒。
