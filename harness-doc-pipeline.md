# 需求到产品的文档链路：Harness 的文档契约

> 问题：从业务需求到最终产品交付，中间需要哪些文档？
> Harness 如何保证这个链路的完整性和可追溯性？
> Agent 如何沿着这条链路逐步推导、生成、执行？

---

## 一、核心场景

```
业务方                                     用户
   │                                         │
   ▼                                         ▼
┌──────────────────────────────────────────────────────────────┐
│                     Agent 工作流                              │
│                                                              │
│  需求阶段     产品阶段       技术阶段        实现阶段    交付阶段│
│  ┌──────┐   ┌────────┐   ┌────────┐   ┌────────┐   ┌────┐  │
│  │业务需求│──▶│产品文档 │──▶│技术文档 │──▶│实现文档 │──▶│交付│  │
│  │用户故  │   │PRD     │   │架构设计│   │Sprint  │   │文档│  │
│  │事     │   │原型    │   │API契约 │   │Contract│   │    │  │
│  │       │   │        │   │DB设计  │   │任务拆解│   │    │  │
│  └──────┘   └────────┘   └────────┘   └────────┘   └────┘  │
│       │           │           │           │          │       │
│       └───────────┴───────────┴───────────┴──────────┘       │
│                  逐级推导，后一级依赖前一级                      │
└──────────────────────────────────────────────────────────────┘
```

**关键点：** Agent 不是直接写代码的。它的工作是把上一级的文档消化后，产出下一级的文档，最后才写代码。每一级文档都是下一级的"输入规范"。

---

## 二、完整的文档链

### 第 1 层：需求文档（业务视角）

| 文档 | 内容 | 产出者 | 消费者 |
|------|------|--------|--------|
| **业务需求说明书** | 业务背景、目标用户、核心诉求、成功标准 | 业务方 / PM | Agent（产品阶段） |
| **用户故事** | 作为 XX，我想要 YY，以便 ZZ | PM + Agent | Agent（产品阶段） |
| **验收标准（业务级）** | 从业务角度什么算"做完了" | 业务方 | Agent（产品+技术阶段） |

**Harness 要保证的：**
- 每个需求有明确的"为什么做"和"什么算做完"
- 没有模糊不清的描述（Agent 遇到模糊项 → 标记并追问）

### 第 2 层：产品文档（产品视角）

| 文档 | 内容 | 产出者 | 消费者 |
|------|------|--------|--------|
| **PRD（产品需求文档）** | 功能列表、优先级、版本规划、交互流程 | Agent + PM | Agent（技术阶段） |
| **功能规格说明书** | 每个功能的详细行为、边界条件、异常流程 | Agent | Agent（技术阶段） |
| **交互原型/线框图** | 页面布局、用户操作路径（若适用） | Agent + Designer | Agent（前端实现） |

**Harness 要保证的：**
- PRD 中的每个功能点都能追踪到对应的业务需求
- 功能规格足够详细，能支撑技术设计
- 没有"到时候再说"的技术盲区

### 第 3 层：技术文档（技术视角）

| 文档 | 内容 | 产出者 | 消费者 |
|------|------|--------|--------|
| **架构设计文档** | 系统架构图、模块划分、技术选型、数据流 | Agent（architect-lead） | Agent（实现阶段） |
| **API 契约** | 接口定义、请求/响应格式、状态码、错误处理 | Agent（backend-dev） | Agent（前后端） |
| **数据库设计** | ER 图、表结构、索引设计、迁移计划 | Agent（database-dev） | Agent（实现阶段） |
| **组件设计** | 前端组件树、状态管理、路由设计 | Agent（frontend-dev） | Agent（实现阶段） |

**Harness 要保证的：**
- 技术文档完整覆盖 PRD 的所有功能点
- API 契约前后端一致（Generator 协调）
- 架构决策记录（ADR）——为什么选这个方案

### 第 4 层：实现文档（执行视角）

| 文档 | 内容 | 产出者 | 消费者 |
|------|------|--------|--------|
| **Sprint Contract** | 本轮做什么、不做什么、验收标准 | Generator + Evaluator | Generator + Specialists |
| **任务拆解清单** | 按模块/层级拆分的实施步骤 | Generator | Specialists |
| **测试用例** | 单元测试、集成测试、E2E 测试覆盖 | Agent | Agent（验证阶段） |

**Harness 要保证的：**
- Sprint Contract 的所有验收标准可测试
- 任务拆解覆盖所有需要修改的文件
- 测试用例覆盖正常流程 + 边界条件 + 异常流程

### 第 5 层：交付文档（用户视角）

| 文档 | 内容 | 产出者 | 消费者 |
|------|------|--------|--------|
| **用户手册** | 功能使用说明、常见问题 | Agent | 最终用户 |
| **发布说明** | 变更日志、兼容性说明、升级指南 | Agent | 运维 / 用户 |
| **运维文档** | 部署步骤、监控指标、故障处理 | Agent | 运维团队 |

**Harness 要保证的：**
- 用户手册与代码实现同步更新（Agent 改完代码顺手更新文档）
- 发布说明清晰列出 break change

---

## 三、文档链的约束关系

```
业务需求 ──────── 驱动 ────────▶ PRD
   │                              │
   │  每个功能点必须追踪到需求      │  每个功能规格必须可技术实现
   ▼                              ▼
功能规格 ◀────── 约束 ──────── 架构设计
   │                              │
   │  交互设计约束前端组件设计      │  API 契约约束前后端接口
   ▼                              ▼
API 契约 ◀─────── 对齐 ──────── 前端组件
   │                              │
   │  测试用例覆盖所有规格          │  用户手册描述最终功能
   ▼                              ▼
测试用例 ──────── 验证 ────────▶ 用户手册
```

**Harness 要保证的约束：**
1. **可追踪**：每个技术设计点都能追溯到 PRD 的某个功能点
2. **无跳跃**：不能从需求直接跳到代码，必须经过产品→技术→实现
3. **完整性**：每一层的输出都足够支撑下一层的输入
4. **一致性**：前后端 API 契约、代码实现、用户手册描述的是同一个功能

---

## 四、Harness 如何保证这个链路

### 4.1 目录结构约定

```
docs/
├── requirements/          ← 第 1 层：需求
│   ├── AGENTS.md          ← 告诉 Agent 需求文档的规范
│   ├── 01-业务背景.md
│   └── 02-用户故事.md
│
├── product/               ← 第 2 层：产品
│   ├── AGENTS.md          ← 告诉 Agent 产品文档的规范
│   ├── PRD.md
│   └── specs/
│       └── 功能规格.md
│
├── technical/             ← 第 3 层：技术
│   ├── AGENTS.md          ← 告诉 Agent 技术文档的规范
│   ├── architecture.md
│   ├── api-contracts/
│   ├── database/
│   └── decisions/         ← 架构决策记录
│
├── implementation/        ← 第 4 层：实现
│   ├── AGENTS.md          ← 告诉 Agent 实现文档的规范
│   ├── sprint-contracts/
│   ├── tasks/
│   └── tests/
│
└── delivery/              ← 第 5 层：交付
    ├── AGENTS.md          ← 告诉 Agent 交付文档的规范
    ├── user-manual/
    └── release-notes/
```

### 4.2 每层的 AGENTS.md 定义生成规范

每个目录下的 `AGENTS.md` 告诉 Agent：
- 这个目录的文档有什么格式要求
- 必须包含哪些章节
- 与上一层/下一层的衔接规则

示例（`docs/technical/AGENTS.md`）：

```markdown
# Technical Documentation Guide

## 输入
- docs/product/PRD.md（必须已存在）
- docs/product/specs/（功能规格）

## 输出
- docs/technical/architecture.md（架构设计）
- docs/technical/api-contracts/（API 契约）
- docs/technical/database/（数据库设计）

## 规则
1. 架构设计必须覆盖 PRD 中的所有功能点
2. API 契约必须前后端一致
3. 每次设计决策必须记录在 docs/technical/decisions/ 中
4. 如果发现 PRD 中有技术无法实现的需求，标记并反馈给产品层

## 检查清单
- [ ] 所有 PRD 功能点都有对应的技术方案
- [ ] API 契约前后端对齐
- [ ] 架构决策有记录（ADR）
- [ ] 数据库设计完整
```

### 4.3 Agent 的工作流（Generator 视角）

```
① 读 docs/requirements/    → 理解业务需求
      │
      ▼
② 读/docs/product/AGENTS.md → 知道怎么写产品文档
      │
      ▼
③ 生成/更新 docs/product/  → 产出 PRD、功能规格
      │
      ▼
④ 读 docs/technical/AGENTS.md → 知道怎么写技术文档
      │
      ▼
⑤ 生成/更新 docs/technical/ → 产出架构、API、DB 设计
      │
      ▼
⑥ 生成 sprint contract     → 明确本轮做什么
      │
      ▼
⑦ 执行代码                 → 写代码 + 测试
      │
      ▼
⑧ 更新 docs/delivery/      → 同步用户手册、发布说明
```

### 4.4 Evaluator 的检查点

每一层流转时，Evaluator 检查：

```
需求 → 产品：  PRD 是否完整覆盖了所有业务需求？
产品 → 技术：  架构设计是否能实现所有功能规格？
技术 → 实现：  Sprint Contract 是否覆盖了所有技术设计点？
实现 → 交付：  用户手册是否与代码实现一致？
```

**任何一个检查不通过 → 退回上一层修改，不准跳级。**

---

## 五、总结

Harness 在这个场景里的核心价值是定义了一条 **从需求到产品的文档流水线**：

| 层 | Agent 做的事 | Harness 给的约束 |
|----|------------|----------------|
| 需求 | 理解 + 结构化 | 格式模板 + 验收标准 |
| 产品 | 生成 PRD + 规格 | 文档规范 + 覆盖率检查 |
| 技术 | 架构 + API + DB | 设计模板 + ADR 要求 |
| 实现 | 写代码 + 测试 | Sprint Contract + 验收 |
| 交付 | 用户手册 + 发版说明 | 同步检查（代码改了文档也得改） |

**每一层都有明确的输入、输出、规范和检查点。Agent 不是自由发挥，而是在这条流水线上一步步走完。**
