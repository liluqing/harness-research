# 测试用例: <!-- 功能名 · 切片 X -->

> **说明：** 每个切片对应一份测试用例。覆盖三级：正常流程 + 边界条件 + 异常流程。
> **TDD 铁律：** 本文件是设计参考。Agent 需将用例转化为实际测试代码，并在编码前跑出 RED。

---

## 正常流程

| ID | 场景 | 输入 | 预期输出 | Mock 策略 |
|----|------|------|---------|----------|
| N1 | <!-- 如：创建成功 --> | <!-- 合法参数 --> | <!-- 200 + data --> | <!-- Mock Repository.save --> |
| N2 | <!-- 如：查询列表 --> | <!-- 分页参数 --> | <!-- 分页结果 --> | <!-- Mock Repository.findAll --> |

---

## 边界条件

| ID | 场景 | 输入 | 预期输出 | Mock 策略 |
|----|------|------|---------|----------|
| B1 | <!-- 如：name 为空 --> | name="" | 400 | — |
| B2 | <!-- 如：name 超长 --> | name=65 字符 | 400 | — |
| B3 | <!-- 如：分页边界 --> | page=999 | 空列表 | Mock Repository |

---

## 异常流程

| ID | 场景 | 输入 | 预期输出 | Mock 策略 |
|----|------|------|---------|----------|
| E1 | <!-- 如：重复创建 --> | 已存在的唯一字段 | 409 | Mock Repository 返回已有记录 |
| E2 | <!-- 如：DB 异常 --> | — | 500 | Mock Repository throws |
| E3 | <!-- 如：外部服务超时 --> | — | 降级返回 | Mock 外部调用 |

---

## 检查清单

- [ ] 每个 API 端点都有对应用例
- [ ] 至少覆盖正常 + 边界 + 异常三类
- [ ] Mock 策略明确（Mock 哪个层、什么行为）
